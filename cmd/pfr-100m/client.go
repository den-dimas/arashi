// Package cmd
package cmd

func main() {
	// addr := "localhost:2268"
	//
	// client, err := protocol.NewSCPIClient(addr, 5*time.Second)
	// if err != nil {
	// 	log.Fatalf("Failed to connect to PFR-100M: %v", err)
	// }
	// defer client.Close()
}
